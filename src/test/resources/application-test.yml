spring:
  datasource:
    url: jdbc:postgresql://localhost:15432/postgres

  cloud:
    gcp:
      pubsub:
        emulator-host: localhost:18538
        project-id: project


notify:
  baseUrl: http://localhost:8089  # URL for the wiremock server

uacservice:
  connection:
      port: 18164

queueconfig:
  sms-fulfilment-topic: rm-internal-sms-fulfilment-dummy
